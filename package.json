{
  "name": "knltb-rekentool",
  "version": "1.0.0",
  "description": "KNLTB Padel Rekentool – browser extensie",
  "scripts": {
    "start": "node server.js",
    "sync:ext": "node scripts/sync-extension.js",
    "ext:run:android": "npm run sync:ext && web-ext run -t firefox-android --source-dir extension",
    "ext:build": "npm run sync:ext && web-ext build --source-dir extension --overwrite-dest --artifacts-dir dist",
    "ext:sign": "npm run sync:ext && web-ext sign --source-dir extension --channel=unlisted --artifacts-dir dist",
    "test": "node --test 'tests/**/*.test.js'"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "web-ext": "^10.3.0"
  }
}
